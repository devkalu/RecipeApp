import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  container: {
    elevation: 3,
    shadowColor: "black",
    shadowOpacity: 0.5,
    shadowOffset: { width: 2, height: 2 },
    shadowRadius: 10,
    flex: 1,
  },
});
